\hypertarget{autotoc_md291_src_plugins_ini_crash_test_crash_test_md}{}\section{Crash Test}\label{autotoc_md291_src_plugins_ini_crash_test_crash_test_md}
The following Markdown Shell Recorder test checks that the I\+NI plugin does not crash, if the input file contains invalid characters or is not properly formatted.


\begin{DoxyCode}{0}
\DoxyCodeLine{for file in \$(find src/plugins/ini/crash\_test -regex '.*crash[0-9][0-9]*.ini\$' | sort); do                \(\backslash\)}
\DoxyCodeLine{        cat "\$file" | kdb import user/tests/ini ini 2>\&1 | grep -q 'SIG' \&\& echo "File \$file caused a crash"    \(\backslash\)}
\DoxyCodeLine{        kdb rm -rf user/tests/ini 2>\&1 | grep -q 'SIG' \&\& echo "File \$file caused a crash"                      \(\backslash\)}
\DoxyCodeLine{        \# Check if we successfully removed all keys                                                             \(\backslash\)}
\DoxyCodeLine{        kdb ls user/tests/ini                                                                                   \(\backslash\)}
\DoxyCodeLine{done}
\DoxyCodeLine{\#>}
\end{DoxyCode}
 